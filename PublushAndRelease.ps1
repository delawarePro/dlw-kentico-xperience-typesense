# First Get the versions from the Directory.Build.props in the VersionPrefix xml tag (it should be something like 1.0.25-beta-2)

# Then propose to the user if he want to create a new beta version (increase the number after the beta key => 1.0.25-beta-3) or increment the version the latest number 1.0.26-beta-1

# Now update this number in the Directory.Build.props file

